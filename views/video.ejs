<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Reproducción de Video</title>
</head>
<body>
  <h1>Reproducción de Video</h1>
  <% if (episodeUrl.includes('mega.nz/embed/')) { %>
    <!-- Enlace embebido -->
    <iframe src="<%= episodeUrl %>" width="800" height="450" frameborder="0" allowfullscreen></iframe>
  <% } else { %>
    <!-- Enlace de video directo -->
    <video controls width="800">
      <source src="<%= episodeUrl %>" type="video/mp4">
      Tu navegador no soporta la reproducción de video.
    </video>
  <% } %>
  <br><br>
  <a href="/">Volver</a>
</body>
</html>
